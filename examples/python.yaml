language:
  name: python
  binary: python
  repl: python
devcontainer:
  build:
    args:
      PYTHON_VERSION: "3"
    base: mcr.microsoft.com/devcontainers/python:${PYTHON_VERSION}
  selftest: python -c 'print("Hello World!")'
