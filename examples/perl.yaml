language:
  name: perl
  binary: perl
devcontainer:
  build:
    packages:
      - perl
      - perltidy
      - libperl-languageserver-perl
  selftest: |
    perl -e 'print "Hello World!\n";'
    perltidy -v
vscode:
  extensions:
    - richterger.perl
    - sfodje.perltidy
